/**
 * Created by jason on 10/29/16.
 */
public class ClientData {
    MedicalWeights mw = new MedicalWeights();
    MetaMapWrapper metamap = new MetaMapWrapper(Parser.host);
    Neo4jQuery neo_q = new Neo4jQuery(Parser.host);
}
